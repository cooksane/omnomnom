<div class="col-lg-6 col-lg-offset-3 text-center">
    <div class="panel shadow" id="panel-start">
        <!--div class="subtitle">{{Category}} > {{Subcategory}}</div-->
        <h1 class="title">{{Title}}</h1>
        <h4 class="description">{{Description}}</h4>
        <div class="stats">
            {{TotalMinutes}} Min | {{YieldNumber}} {{YieldUnit}}
        </div>
        <div class="header-image">
            <img id="recipeImage" class="center-block unselectable img-responsive img-rounded" src="{{ImageURL}}" />
        </div>
        <p><a id="continue" class="btn btn-lg" href="javascript:" role="button">Continue</a></p>
        <!-- <button id="continue" type="button" class="btn btn-default pull-right">Continue</button> -->
    </div>
</div>

<!--
<div id="left" class="col-lg-5">
    <img id="recipeImage" class="unselectable img-responsive" src="{{ImageURL}}" />
</div>

<div id="right" class="col-lg-7">

    <div class="row">
        <h1>{{Title}}</h1>
        <p>{{Description}}</p>
    </div>

    <div class="row">
        <button id="continue" type="button" class="btn btn-default pull-right">Begin</button>
    </div>

</div>
-->